export declare const nacos: {
    registry: {
        client: {
            serverList: string;
            namespace: string;
        };
    };
    config: {
        client: {
            serverAddr: string;
        };
    };
};
